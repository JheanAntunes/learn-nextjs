import React from "react";

const PageAula = ({ params, searchParams }) => {
  console.log("params: ", params.aula);
  console.log("searchparams: ", searchParams.curso);
  return <main>dasdsads</main>;
};

export default PageAula;
